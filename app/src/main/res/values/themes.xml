<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Theme.NoteIt" parent="Theme.Material3.Light.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimaryVariant">@color/cinder</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondaryVariant">@color/ultramarineBlue</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" >@color/white</item>

        <!-- Window & Toolbar Background color -->
        <item name="android:windowBackground">@color/white</item>

        <!-- Text Color -->
        <item name="android:textColorPrimary">@color/black</item>

        <!-- Text color hint -->
        <item name="android:textColorHint">#777777</item>

        <!-- Icon color -->
        <item name="iconTint">@color/black</item>
        <item name="tint">@color/black</item>

        <!-- Searchbar color -->
        <item name="colorPrimary">@color/seashell</item>

        <!-- Card background color -->
        <item name="colorSecondary">@color/seashell</item>

        <item name="strokeColor">@color/grey</item>





    </style>
</resources>